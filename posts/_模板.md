---
title: 文章标题（必填）
date: 2026-01-01
tags: [JavaScript, CSS]
summary: 列表页显示的一句话摘要。不写的话会自动截取正文第一段。
# slug: custom-url        # 可选。默认用文件名，建议用英文/数字/短横线
# draft: true             # 可选。true 表示草稿，构建时跳过
---

正文从这里开始，正常写 Markdown 就行。

## 二级标题

- 列表项
- 行内代码写作 `code`

```js
// 代码块会自动高亮外面的样式（灰色底 + 等宽字体）
const a = 1;
```

> 引用块也可以。

写完这篇后运行：

```bash
npm run build:blog
```

然后 `blog/<slug>.html` 和两个列表就都更新了。

**注意**：以 `_` 开头的文件（包括本文件）会被跳过，不会被构建成文章。
